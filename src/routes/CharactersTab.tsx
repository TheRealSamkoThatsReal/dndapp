import { EmptyState } from '../ui/kit'

export default function CharactersTab() {
  return (
    <EmptyState
      icon="🛡️"
      title="Character sheets — coming soon"
      hint="Full 5e sheets: abilities, skills, HP/AC, spell slots, inventory, and rests — droppable straight into the combat tracker."
    />
  )
}
