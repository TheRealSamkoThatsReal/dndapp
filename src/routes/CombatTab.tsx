import { EmptyState } from '../ui/kit'

export default function CombatTab() {
  return (
    <EmptyState
      icon="⚔️"
      title="Combat tracker — coming next"
      hint="Initiative order, HP/AC, conditions with round countdowns, and one-tap monster drops from your wiki. Building this next."
    />
  )
}
